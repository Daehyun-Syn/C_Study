#include <stdio.h>

int main() {

	int a;
	printf("입력하세요 : ");
		scanf("%d",&a);
		if (a >= 60) {
			printf("합격입니다");
		}
		if (a < 60) {
			printf("불합격입니다");
		}
	}
		else if (a < 0 || a>100) {
			printf("오류입니다");
		}else {
	}
